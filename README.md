# Пульт охраны банка

Данный скрипт представлен для сотрудников банка. Пульт охраны банка - сайт, который можно подключить к удаленной базе данных с визитами и карточками пропуска сотрудников нашего банка. 

Фиксированные посещения выглядят следующим образом:

[![bank.jpg](https://i.postimg.cc/mghgbGfT/bank.jpg)](https://postimg.cc/G4wCQVmf)

## Как установить
Операционная система: любая, где доступен Python 3 (Windows, macOS, Linux). Используем 'pip' для установки зависимостей (requests, python-dotenv):

```
pip install -r requirements.txt
```

Рекомендуется использовать virtualenv/env для изоляции проекта.

## Конфигурация

Программа использует переменные окружения для настройки подключения к базе данных и других параметров. Это позволяет настраивать программу без изменения Python-кода. Переменные можно задавать через файл .env (дефолтные значения) или аргументы командной строки для переопределения.
Для временного переопределения настроек используйте аргументы командной строки. Они имеют приоритет над .env. Запустите python main.py --help для списка аргументов.

Полный список аргументов:

1) --db-host: Хост базы данных.
2) --db-port: Порт базы данных.
3) --db-name: Имя базы данных.
4) --db-user: Пользователь базы данных.
5) --db-password: Пароль базы данных.
6) --db-secret-key: Секретный ключ Django.

Примеры запуска программы:

1) Использует дефолты из .env:
```
python main.py
```

2) Переопределяет пользователя и пароль:
```
python main.py --db-user newuser --db-password newpass
``` 

## Переменные окружения
Программа использует нестандартные переменные окружения для настройки. Эти переменные не могут быть автоматически обнаружены и должны быть указаны в файле `.env` перед запуском программы.

**DB_HOST** - адрес в settings.py для подключения базы данных. Форматом является строка (например, `checkpoint.programmer.org`).

**DB_PORT** - порт базы данных. Формат: строка (например, `5494`).

**DB_NAME** - имя базы данных. Формат: строка (например, `check`).

**DB_USER** - пользователь базы данных. Формат: строка (например, `programmer`).

**DB_PASSWORD** - пароль в settings.py для подключения базы данных. Форматом является строка (например, `programmer01`).

**DB_SECRET_KEY** - секретный ключ в settings.py для подключения базы данных. Форматом является строка (например, `PROG_ME`).
